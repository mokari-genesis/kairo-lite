'use client'

/**
 * @deprecated Este hook ha sido movido a usuarioContext.tsx
 * Usa `useCurrentUser` desde '@/app/usuarioContext' en su lugar
 *
 * Este archivo se mantiene solo para compatibilidad hacia atrás
 * y será eliminado en una versión futura.
 */
export { useCurrentUser } from '../usuarioContext'
